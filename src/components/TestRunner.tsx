/**
 * TestRunner — SCAFFOLD ONLY.
 *
 * Phase 1: one-item-per-screen sequencer, audio-first instructions with replay control, performance.now() latency capture anchored to the audio 'ended' event (BOTH first-ever and most-recent anchors, per migration 0003), Pointer Events for input modality, input disabled during playback, lock-on-advance with edit-before-advance.
 * Deliberately unimplemented in Phase 0. See PHASE_0_REPORT.md.
 */

export default function TestRunner() {
  return null;
}
