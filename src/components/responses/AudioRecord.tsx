/**
 * AudioRecord — SCAFFOLD ONLY.
 *
 * Phase 1: AUDIO_RECORD response input. MUST probe MediaRecorder.isTypeSupported() - iOS Safari produces MP4/AAC, Chrome/Android WebM/Opus. Do not assume one format.
 * Deliberately unimplemented in Phase 0. See PHASE_0_REPORT.md.
 */

export default function AudioRecord() {
  return null;
}
