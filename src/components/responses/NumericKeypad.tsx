/**
 * NumericKeypad — SCAFFOLD ONLY.
 *
 * Phase 1: NUMERIC_KEYPAD response input. On-screen digits only - never a native keyboard (avoids the typing-skill confound).
 * Deliberately unimplemented in Phase 0. See PHASE_0_REPORT.md.
 */

export default function NumericKeypad() {
  return null;
}
