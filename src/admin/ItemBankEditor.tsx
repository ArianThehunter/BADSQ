/**
 * ItemBankEditor — SCAFFOLD ONLY.
 *
 * Phase 2: versioned item-bank editor. MUST refuse to activate an item while is_stimulus_replayable IS NULL (Domain 4 decision still pending).
 * Deliberately unimplemented in Phase 0. See PHASE_0_REPORT.md.
 */

export default function ItemBankEditor() {
  return null;
}
